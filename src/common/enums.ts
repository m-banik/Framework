export enum NotificationsEnum {
  wrongData = "The given data didn't meet the expectations",
  modalHasNoChildren = 'Modal has no children',
}

export enum DefaultValuesEnum {
  emptyString = '',
  InputName = 'Input',
  ButtonName = 'Button',
}

export enum InputKindsEnum {
  color = 'color',
  button = 'button',
  checkbox = 'checkbox',
  date = 'date',
  email = 'email',
  file = 'file',
  hidden = 'hidden',
  image = 'image',
  month = 'month',
  number = 'number',
  password = 'password',
  radio = 'radio',
  range = 'range',
  reset = 'reset',
  search = 'search',
  submit = 'submit',
  tel = 'tel',
  text = 'text',
  time = 'time',
  url = 'url',
  week = 'week',
  dateTimeLocal = 'datetime-local',
  textarea = 'textarea',
}
